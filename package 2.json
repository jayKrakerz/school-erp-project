{
  "name": "school-erp-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "server": "python3 backend/server.py",
    "launch": "npm run dev & npm run server",
    "install-all": "npm install --prefix frontend"
  }
}
